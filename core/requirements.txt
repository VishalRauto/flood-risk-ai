# ── HTTP / async HTTP ────────────────────────────────────────────────────────
requests==2.34.2
aiohttp==3.13.5
httpx==0.28.1

# ── FastAPI server ────────────────────────────────────────────────────────────
fastapi==0.115.14
uvicorn[standard]==0.32.1

# ── AI providers ─────────────────────────────────────────────────────────────
h2ogpte==1.6.41rc3
openai==1.107.1

# ── MCP ──────────────────────────────────────────────────────────────────────
fastmcp==1.0
mcp-server-time~=2025.8

# ── NVIDIA Agent Toolkit (NAT) ────────────────────────────────────────────────
# Used by nat_base.py and test_workflow.py (graceful ImportError fallback)
nvidia-nat==1.3.0a20250917
nvidia-nat-langchain==1.3.0a20250917
nvidia-nat[mcp]==1.3.0a20250917

# ── Settings / config ────────────────────────────────────────────────────────
pydantic-settings==2.10.1

# ── Task queue ───────────────────────────────────────────────────────────────
rq==2.1.0

# ── HTML parsing (local NIM provider) ────────────────────────────────────────
beautifulsoup4==4.15.0

# ── Auth ─────────────────────────────────────────────────────────────────────
pyjwt==2.10.1

# ── Logging ──────────────────────────────────────────────────────────────────
structlog==23.2.0

# ── SMS Alerts (Twilio) ───────────────────────────────────────────────────────
twilio==9.6.5

# ── PDF Report Generation ─────────────────────────────────────────────────────
reportlab==4.2.5
Pillow==10.4.0

# ── Research / ML ─────────────────────────────────────────────────────────────
# Numerical computing and ML (CPU-only torch keeps image size reasonable)
numpy==1.26.4
scikit-learn==1.5.2
scipy==1.14.1
statsmodels==0.14.4
# PyTorch CPU — required for LSTM / GRU / Transformer models
# Install separately via Dockerfile to pick the right index URL
# torch==2.4.1+cpu  ← added in Dockerfile via --extra-index-url
joblib==1.4.2
